#ifndef POLYNOMIALS_H
#define POLYNOMIALS_H

// Berechnet das Polynom: f1(x) = x^2 + 2x + 1
int poly1(int x);

// Berechnet das Polynom: f2(x) = 3x^3 - 4x + 5
int poly2(int x);

// Berechnet das Polynom: f3(x) = 2x^2 - x + 3
int poly3(int x);

#endif // POLYNOMIALS_H
